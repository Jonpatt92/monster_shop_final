<% if @password_change %>
  <%= render partial: "edit_password", locals: { object: @user, button: 'Change Password'} %>
<% elsif !@password_change %>
  <%= render partial: "edit_profile", locals: { object: @user, button: 'Update Profile'} %>
<% end %>
